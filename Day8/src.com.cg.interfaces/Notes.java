package src.com.cg.interfaces;

public class Notes {

	//Interface
	//1.looks  like a cls but it is not a cls
	//2.keyword-interface. only cls signature
	//3. can have only abstract method
	//4. when var is declared, default accessibility would be public static final
	//5. method- public abstract
	//6. class- takes properties  and behavior of interface = implementable cls
	//keyword implements
	
	//extends - class-class / interface-interface
	//implements - class-interface
	//interface-class -impossible
}
